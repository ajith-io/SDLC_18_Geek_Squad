#ifndef __BMI__CALCULATOR_C
#define __BMI__CALCULATOR_C
/**
 * @brief creating a bmi calculating function 
 *
 * 
 * @param x taking value of height(in cm)
 * @param y taking value of weight(in kg)
 * @return int 
 */

float bmi_calculator_Us(int x, int y);
float bmi_calculator_Metric(int x, int y);

#endif